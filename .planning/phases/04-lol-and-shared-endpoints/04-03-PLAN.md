---
phase: 04-lol-and-shared-endpoints
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/whisper/src/lol/clash-v1.ts
  - packages/whisper/src/lol/clash-v1.test.ts
  - packages/whisper/src/lol/league-v4.ts
  - packages/whisper/src/lol/league-v4.test.ts
  - packages/whisper/src/lol/league-exp-v4.ts
  - packages/whisper/src/lol/league-exp-v4.test.ts
  - packages/whisper/src/lol/lol-challenges-v1.ts
  - packages/whisper/src/lol/lol-challenges-v1.test.ts
autonomous: true
requirements:
  - ENDP-01

must_haves:
  truths:
    - "clash-v1 has 4 methods: getPlayersByPuuid, getTournaments, getTournamentById, getTeamById"
    - "league-v4 has 6 methods: getChallengerLeague, getGrandmasterLeague, getMasterLeague, getEntriesByPuuid, getEntries, getById"
    - "league-exp-v4 has 1 method: getEntries (reuses LolLeagueEntry type from league-v4)"
    - "lol-challenges-v1 has 6 methods: getConfig, getPercentiles, getChallengeConfig, getChallengePercentiles, getChallengeLeaderboard, getPlayerData"
    - "All 4 modules use PlatformRoute"
  artifacts:
    - path: "packages/whisper/src/lol/clash-v1.ts"
      provides: "clashV1 namespace"
      exports: ["clashV1"]
    - path: "packages/whisper/src/lol/league-v4.ts"
      provides: "leagueV4 namespace"
      exports: ["leagueV4"]
    - path: "packages/whisper/src/lol/league-exp-v4.ts"
      provides: "leagueExpV4 namespace"
      exports: ["leagueExpV4"]
    - path: "packages/whisper/src/lol/lol-challenges-v1.ts"
      provides: "lolChallengesV1 namespace"
      exports: ["lolChallengesV1"]
  key_links:
    - from: "packages/whisper/src/lol/league-v4.ts"
      to: "packages/whisper/src/types/overrides/lol-league.ts"
      via: "LeagueList import"
      pattern: "import.*LeagueList.*overrides/lol-league"
    - from: "packages/whisper/src/lol/clash-v1.ts"
      to: "packages/whisper/src/types/overrides/lol-clash.ts"
      via: "ClashTeam import"
      pattern: "import.*ClashTeam.*overrides/lol-clash"
    - from: "packages/whisper/src/lol/lol-challenges-v1.ts"
      to: "packages/whisper/src/types/overrides/lol-challenges.ts"
      via: "ApexPlayerInfo import"
      pattern: "import.*ApexPlayerInfo.*overrides/lol-challenges"
---

<objective>
Implement 4 Platform-routed LoL endpoint modules that use override types (clash, league, league-exp, challenges) with co-located tests.

Purpose: These modules depend on the override types created in Plan 01 (LeagueList, ClashTeam, ApexPlayerInfo). They have more methods (17 total) and more complex parameter signatures (queue/tier/division for league entries).
Output: 4 endpoint modules + 4 test files.
</objective>

<execution_context>
@/Users/wardbox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wardbox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lol-and-shared-endpoints/04-CONTEXT.md
@.planning/phases/04-lol-and-shared-endpoints/04-RESEARCH.md
@.planning/phases/04-lol-and-shared-endpoints/04-01-SUMMARY.md

<interfaces>
From packages/whisper/src/core/client.ts:
```typescript
export interface WhisperClient {
  request<T>(route: PlatformRoute | RegionalRoute, path: string, methodId: string, options?: RequestOptions): Promise<ApiResponse<T>>;
}
```

From packages/whisper/src/types/platform.ts:
```typescript
export type PlatformRoute = 'na1' | 'br1' | 'la1' | 'la2' | 'jp1' | 'kr' | 'me1' | 'eun1' | 'euw1' | 'tr1' | 'ru' | 'oc1' | 'ph2' | 'sg2' | 'th2' | 'tw2' | 'vn2';
```

From packages/whisper/src/types/generated/lol.ts:
```typescript
export interface LolLeagueEntry { /* league entry fields */ }
export interface ChallengeConfigInfo { /* challenge config fields */ }
export interface ChallengePercentiles { /* percentile map */ }
export interface PlayerInfo { /* player challenge data */ }
export interface Tournament { /* clash tournament */ }
```

From packages/whisper/src/types/overrides/lol-league.ts (Plan 01):
```typescript
export interface LeagueList { leagueId: string; entries: LolLeagueEntry[]; tier: string; name: string; queue: string; }
```

From packages/whisper/src/types/overrides/lol-clash.ts (Plan 01):
```typescript
export interface ClashTeam { id: string; tournamentId: number; name: string; iconId: number; tier: number; captain: string; abbreviation: string; players: ClashTeamPlayer[]; }
```

From packages/whisper/src/types/overrides/lol-challenges.ts (Plan 01):
```typescript
export interface ApexPlayerInfo { puuid: string; value: number; position: number; }
```

Registry endpoints:
- clash-v1: 4 endpoints (getPlayersByPuuid, getTournaments, getTournamentById, getTeamById), all Platform
- league-v4: 6 endpoints (getChallengerLeague, getGrandmasterLeague, getMasterLeague, getEntriesByPuuid, getEntries, getById), all Platform
- league-exp-v4: 1 endpoint (getEntries), Platform, reuses LolLeagueEntry
- lol-challenges-v1: 6 endpoints, all Platform
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement clash-v1 and league-v4 modules with tests</name>
  <files>
    packages/whisper/src/lol/clash-v1.ts
    packages/whisper/src/lol/clash-v1.test.ts
    packages/whisper/src/lol/league-v4.ts
    packages/whisper/src/lol/league-v4.test.ts
    packages/whisper/src/lol/league-exp-v4.ts
    packages/whisper/src/lol/league-exp-v4.test.ts
  </files>
  <read_first>
    packages/whisper/src/core/client.ts
    packages/whisper/src/types/generated/lol.ts
    packages/whisper/src/types/overrides/lol-league.ts
    packages/whisper/src/types/overrides/lol-clash.ts
    packages/whisper/src/types/platform.ts
    scripts/generate-schema/registry.ts
  </read_first>
  <action>
Create 3 endpoint modules, all PlatformRoute.

**clash-v1.ts** -- `export const clashV1`:
Imports: `WhisperClient` from `'../core/client.js'`, `PlatformRoute` from `'../types/platform.js'`, `PlayerInfo` and `Tournament` from `'../types/generated/lol.js'`, `ClashTeam` from `'../types/overrides/lol-clash.js'`.

1. `getPlayersByPuuid(client, route, puuid)` -> `Promise<PlayerInfo[]>` -- path: `/lol/clash/v1/players/by-puuid/${puuid}`, methodId: `clash-v1.getPlayersByPuuid`
2. `getTournaments(client, route)` -> `Promise<Tournament[]>` -- path: `/lol/clash/v1/tournaments`, methodId: `clash-v1.getTournaments`
3. `getTournamentById(client, route, tournamentId: string)` -> `Promise<Tournament>` -- path: `/lol/clash/v1/tournaments/${tournamentId}`, methodId: `clash-v1.getTournamentById`
4. `getTeamById(client, route, teamId: string)` -> `Promise<ClashTeam>` -- path: `/lol/clash/v1/teams/${teamId}`, methodId: `clash-v1.getTeamById`

Note: Registry shows `PlayerDTO` maps to generated `PlayerInfo`. Check the actual generated type name.

**league-v4.ts** -- `export const leagueV4`:
Imports: `WhisperClient`, `PlatformRoute`, `LolLeagueEntry` from generated, `LeagueList` from `'../types/overrides/lol-league.js'`.

1. `getChallengerLeague(client, route, queue: string)` -> `Promise<LeagueList>` -- path: `/lol/league/v4/challengerleagues/by-queue/${queue}`, methodId: `league-v4.getChallengerLeague`
2. `getGrandmasterLeague(client, route, queue: string)` -> `Promise<LeagueList>` -- path: `/lol/league/v4/grandmasterleagues/by-queue/${queue}`, methodId: `league-v4.getGrandmasterLeague`
3. `getMasterLeague(client, route, queue: string)` -> `Promise<LeagueList>` -- path: `/lol/league/v4/masterleagues/by-queue/${queue}`, methodId: `league-v4.getMasterLeague`
4. `getEntriesByPuuid(client, route, puuid: string)` -> `Promise<LolLeagueEntry[]>` -- path: `/lol/league/v4/entries/by-puuid/${puuid}`, methodId: `league-v4.getEntriesByPuuid`
5. `getEntries(client, route, queue: string, tier: string, division: string, options?: { page?: number })` -> `Promise<LolLeagueEntry[]>` -- path: `/lol/league/v4/entries/${queue}/${tier}/${division}`, methodId: `league-v4.getEntries`. If `options.page` provided, pass as query param.
6. `getById(client, route, leagueId: string)` -> `Promise<LeagueList>` -- path: `/lol/league/v4/leagues/${leagueId}`, methodId: `league-v4.getById`

**league-exp-v4.ts** -- `export const leagueExpV4`:
Imports: `WhisperClient`, `PlatformRoute`, `LolLeagueEntry` from generated.

1. `getEntries(client, route, queue: string, tier: string, division: string, options?: { page?: number })` -> `Promise<LolLeagueEntry[]>` -- path: `/lol/league-exp/v4/entries/${queue}/${tier}/${division}`, methodId: `league-exp-v4.getEntries`. If `options.page` provided, pass as query param.

All modules: TSDoc on namespace + every method, `as const`, `.js` import extensions.

**Tests:** mockClient pattern. For each method: verify return value, call args (route, path, methodId). For methods with query params: test with and without options. `@ts-expect-error` for RegionalRoute on each module.
  </action>
  <verify>
    <automated>cd /Users/wardbox/git/whisper && pnpm vitest run src/lol/clash-v1.test.ts src/lol/league-v4.test.ts src/lol/league-exp-v4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - clash-v1.ts contains `export const clashV1 =` with 4 methods
    - clash-v1.ts imports ClashTeam from `'../types/overrides/lol-clash.js'`
    - league-v4.ts contains `export const leagueV4 =` with 6 methods
    - league-v4.ts imports LeagueList from `'../types/overrides/lol-league.js'`
    - league-exp-v4.ts contains `export const leagueExpV4 =` with 1 method
    - All 3 modules import PlatformRoute
    - All 3 modules have `as const` and `@example` TSDoc
    - All 3 test files have `@ts-expect-error` for regional route
    - `pnpm vitest run` for all 3 test files passes
  </acceptance_criteria>
  <done>3 endpoint modules with 11 total methods, all PlatformRoute, override types wired correctly, all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lol-challenges-v1 module with tests</name>
  <files>
    packages/whisper/src/lol/lol-challenges-v1.ts
    packages/whisper/src/lol/lol-challenges-v1.test.ts
  </files>
  <read_first>
    packages/whisper/src/core/client.ts
    packages/whisper/src/types/generated/lol.ts
    packages/whisper/src/types/overrides/lol-challenges.ts
    packages/whisper/src/types/platform.ts
    scripts/generate-schema/registry.ts
  </read_first>
  <action>
Create the lol-challenges-v1 endpoint module, which has 6 methods and uses both generated and override types.

**lol-challenges-v1.ts** -- `export const lolChallengesV1`:
Imports: `WhisperClient` from `'../core/client.js'`, `PlatformRoute` from `'../types/platform.js'`, `ChallengeConfigInfo`, `ChallengePercentiles`, `PlayerInfo` from `'../types/generated/lol.js'`, `ApexPlayerInfo` from `'../types/overrides/lol-challenges.js'`.

1. `getConfig(client, route)` -> `Promise<ChallengeConfigInfo[]>` -- path: `/lol/challenges/v1/challenges/config`, methodId: `lol-challenges-v1.getConfig`
2. `getPercentiles(client, route)` -> `Promise<ChallengePercentiles>` -- path: `/lol/challenges/v1/challenges/percentiles`, methodId: `lol-challenges-v1.getPercentiles`
3. `getChallengeConfig(client, route, challengeId: number)` -> `Promise<ChallengeConfigInfo>` -- path: `/lol/challenges/v1/challenges/${challengeId}/config`, methodId: `lol-challenges-v1.getChallengeConfig`
4. `getChallengePercentiles(client, route, challengeId: number)` -> `Promise<Record<string, number>>` -- path: `/lol/challenges/v1/challenges/${challengeId}/percentiles`, methodId: `lol-challenges-v1.getChallengePercentiles`. Note: The registry names this `ChallengePercentileEntry` but the response is a map of level to percentile value. The generated `ChallengePercentiles` type is the aggregate map for all challenges; per-challenge percentiles is a simpler `Record<string, number>`. Verify the exact shape from the Riot API explorer if possible.
5. `getChallengeLeaderboard(client, route, challengeId: number, level: string)` -> `Promise<ApexPlayerInfo[]>` -- path: `/lol/challenges/v1/challenges/${challengeId}/leaderboards/by-level/${level}`, methodId: `lol-challenges-v1.getChallengeLeaderboard`
6. `getPlayerData(client, route, puuid: string)` -> `Promise<PlayerInfo>` -- path: `/lol/challenges/v1/player-data/${puuid}`, methodId: `lol-challenges-v1.getPlayerData`

TSDoc on namespace + every method, `as const`, `.js` imports.

**Test:** mockClient pattern. Test all 6 methods: verify return value, call args. `@ts-expect-error` for RegionalRoute.
  </action>
  <verify>
    <automated>cd /Users/wardbox/git/whisper && pnpm vitest run src/lol/lol-challenges-v1.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lol-challenges-v1.ts contains `export const lolChallengesV1 =` with 6 methods
    - lol-challenges-v1.ts imports ApexPlayerInfo from `'../types/overrides/lol-challenges.js'`
    - lol-challenges-v1.ts imports ChallengeConfigInfo, ChallengePercentiles, PlayerInfo from generated
    - Module has `as const` and `@example` TSDoc on every method
    - Test file has `@ts-expect-error` for regional route
    - `pnpm vitest run src/lol/lol-challenges-v1.test.ts` passes
  </acceptance_criteria>
  <done>lol-challenges-v1 module with 6 methods, all PlatformRoute, override + generated types wired, all tests green.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lol/clash-v1.test.ts src/lol/league-v4.test.ts src/lol/league-exp-v4.test.ts src/lol/lol-challenges-v1.test.ts` -- all pass
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- 4 endpoint namespace objects, 17 total methods
- All use PlatformRoute
- Override types correctly imported and used
- All tests pass including type tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-lol-and-shared-endpoints/04-03-SUMMARY.md`
</output>
