---
phase: 04-lol-and-shared-endpoints
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/whisper/src/lol/champion-mastery-v4.ts
  - packages/whisper/src/lol/champion-mastery-v4.test.ts
  - packages/whisper/src/lol/champion-v3.ts
  - packages/whisper/src/lol/champion-v3.test.ts
  - packages/whisper/src/lol/summoner-v4.ts
  - packages/whisper/src/lol/summoner-v4.test.ts
  - packages/whisper/src/lol/lol-status-v4.ts
  - packages/whisper/src/lol/lol-status-v4.test.ts
  - packages/whisper/src/lol/spectator-v5.ts
  - packages/whisper/src/lol/spectator-v5.test.ts
autonomous: true
requirements:
  - ENDP-01

must_haves:
  truths:
    - "champion-mastery-v4 has 4 methods: getByPuuid, getTopByPuuid, getByPuuidByChampion, getScoresByPuuid"
    - "champion-v3 has 1 method: getChampionRotations"
    - "summoner-v4 has exactly 2 methods: getByPuuid, getByAccountId (removed endpoints excluded)"
    - "lol-status-v4 has 1 method: getStatus"
    - "spectator-v5 has 2 methods: getCurrentGame, getFeaturedGames"
    - "All 5 modules use PlatformRoute, not RegionalRoute"
  artifacts:
    - path: "packages/whisper/src/lol/champion-mastery-v4.ts"
      provides: "championMasteryV4 namespace"
      exports: ["championMasteryV4"]
    - path: "packages/whisper/src/lol/champion-v3.ts"
      provides: "championV3 namespace"
      exports: ["championV3"]
    - path: "packages/whisper/src/lol/summoner-v4.ts"
      provides: "summonerV4 namespace"
      exports: ["summonerV4"]
    - path: "packages/whisper/src/lol/lol-status-v4.ts"
      provides: "lolStatusV4 namespace"
      exports: ["lolStatusV4"]
    - path: "packages/whisper/src/lol/spectator-v5.ts"
      provides: "spectatorV5 namespace"
      exports: ["spectatorV5"]
  key_links:
    - from: "packages/whisper/src/lol/summoner-v4.ts"
      to: "client.request"
      via: "WhisperClient.request<LolSummoner>()"
      pattern: "client\\.request<LolSummoner>"
    - from: "packages/whisper/src/lol/spectator-v5.ts"
      to: "packages/whisper/src/types/overrides/lol-spectator.ts"
      via: "FeaturedGames import"
      pattern: "import.*FeaturedGames.*overrides/lol-spectator"
---

<objective>
Implement 5 Platform-routed LoL endpoint modules (simple GET endpoints) with co-located tests.

Purpose: These are the simpler endpoint modules -- all GET-only, all Platform-routed, with mostly existing generated types. Establishes the pattern for remaining modules.
Output: 5 endpoint modules + 5 test files covering champion-mastery-v4, champion-v3, summoner-v4, lol-status-v4, spectator-v5.
</objective>

<execution_context>
@/Users/wardbox/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wardbox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lol-and-shared-endpoints/04-CONTEXT.md
@.planning/phases/04-lol-and-shared-endpoints/04-RESEARCH.md
@.planning/phases/04-lol-and-shared-endpoints/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/whisper/src/core/client.ts:
```typescript
export interface WhisperClient {
  request<T>(
    route: PlatformRoute | RegionalRoute,
    path: string,
    methodId: string,
    options?: RequestOptions,
  ): Promise<ApiResponse<T>>;
}
```

From packages/whisper/src/types/platform.ts:
```typescript
export type PlatformRoute = 'na1' | 'br1' | 'la1' | 'la2' | 'jp1' | 'kr' | 'me1' | 'eun1' | 'euw1' | 'tr1' | 'ru' | 'oc1' | 'ph2' | 'sg2' | 'th2' | 'tw2' | 'vn2';
```

From packages/whisper/src/types/generated/lol.ts (relevant exports):
```typescript
export interface ChampionMastery { championId: number; championLevel: number; championPoints: number; /* ... */ }
export interface ChampionInfo { freeChampionIds: number[]; freeChampionIdsForNewPlayers: number[]; maxNewPlayerLevel: number; }
export interface LolSummoner { accountId: string; id: string; name: string; profileIconId: number; puuid: string; revisionDate: number; summonerLevel: number; }
export interface LolPlatformData { /* ... status data ... */ }
export interface LolCurrentGameInfo { /* ... spectator data ... */ }
```

From packages/whisper/src/types/overrides/lol-spectator.ts (created by Plan 01):
```typescript
export interface FeaturedGames { gameList: FeaturedGameInfo[]; clientRefreshInterval: number; }
```

Registry endpoint definitions (from registry.ts):
- champion-mastery-v4: 4 endpoints, all Platform, paths /lol/champion-mastery/v4/...
- champion-v3: 1 endpoint, Platform, path /lol/platform/v3/champion-rotations
- summoner-v4: 2 endpoints (getByPuuid, getByAccountId), Platform -- getBySummonerName/Id REMOVED
- lol-status-v4: 1 endpoint, Platform, path /lol/status/v4/platform-data
- spectator-v5: 2 endpoints, Platform, paths /lol/spectator/v5/...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement champion-mastery-v4, champion-v3, and summoner-v4 modules with tests</name>
  <files>
    packages/whisper/src/lol/champion-mastery-v4.ts
    packages/whisper/src/lol/champion-mastery-v4.test.ts
    packages/whisper/src/lol/champion-v3.ts
    packages/whisper/src/lol/champion-v3.test.ts
    packages/whisper/src/lol/summoner-v4.ts
    packages/whisper/src/lol/summoner-v4.test.ts
  </files>
  <read_first>
    packages/whisper/src/core/client.ts
    packages/whisper/src/types/generated/lol.ts
    packages/whisper/src/types/platform.ts
    scripts/generate-schema/registry.ts
    .planning/phases/04-lol-and-shared-endpoints/04-RESEARCH.md
  </read_first>
  <action>
Create 3 endpoint modules following the exact namespace object pattern from RESEARCH.md.

**All modules follow this pattern:**
- Import `WhisperClient` from `'../core/client.js'`
- Import `PlatformRoute` from `'../types/platform.js'`
- Import response types from `'../types/generated/lol.js'`
- Export a `const xxxYy = { ... } as const` namespace object
- Each method: `async methodName(client: WhisperClient, route: PlatformRoute, ...params): Promise<ReturnType>` that calls `client.request<ReturnType>(route, path, methodId)` and returns `response.data`
- Full TSDoc on namespace object and every method (description, @param, @returns, @example)
- `.js` extensions on all relative imports

**champion-mastery-v4.ts** -- `export const championMasteryV4`:
1. `getByPuuid(client, route, puuid)` -> `Promise<ChampionMastery[]>` -- path: `/lol/champion-mastery/v4/champion-masteries/by-puuid/${puuid}`, methodId: `champion-mastery-v4.getByPuuid`, isArray so return type is `ChampionMastery[]`
2. `getTopByPuuid(client, route, puuid, options?: { count?: number })` -> `Promise<ChampionMastery[]>` -- path: `/lol/champion-mastery/v4/champion-masteries/by-puuid/${puuid}/top`, methodId: `champion-mastery-v4.getTopByPuuid`. If `options.count` provided, pass as query param: `{ params: { count: String(options.count) } }`
3. `getByPuuidByChampion(client, route, puuid, championId: number)` -> `Promise<ChampionMastery>` -- path: `/lol/champion-mastery/v4/champion-masteries/by-puuid/${puuid}/by-champion/${championId}`, methodId: `champion-mastery-v4.getByPuuidByChampion`, single object (not array)
4. `getScoresByPuuid(client, route, puuid)` -> `Promise<number>` -- path: `/lol/champion-mastery/v4/scores/by-puuid/${puuid}`, methodId: `champion-mastery-v4.getScoresByPuuid`, returns a primitive number

**champion-v3.ts** -- `export const championV3`:
1. `getChampionRotations(client, route)` -> `Promise<ChampionInfo>` -- path: `/lol/platform/v3/champion-rotations`, methodId: `champion-v3.getChampionRotations`

**summoner-v4.ts** -- `export const summonerV4`:
1. `getByPuuid(client, route, puuid)` -> `Promise<LolSummoner>` -- path: `/lol/summoner/v4/summoners/by-puuid/${puuid}`, methodId: `summoner-v4.getByPuuid`
2. `getByAccountId(client, route, encryptedAccountId)` -> `Promise<LolSummoner>` -- path: `/lol/summoner/v4/summoners/by-account/${encryptedAccountId}`, methodId: `summoner-v4.getByAccountId`

CRITICAL: summoner-v4 has ONLY 2 methods. Do NOT include getBySummonerName, getBySummonerId (removed June 2025), or getByMe (RSO-only).

**Tests pattern** (same for all 3 test files):
```typescript
import { describe, expect, it, vi } from 'vitest';
import type { WhisperClient } from '../core/client.js';

function mockClient(data: unknown): WhisperClient {
  return { request: vi.fn().mockResolvedValue({ data, status: 200, headers: {} }) };
}
```
- Test each method verifying: correct return value (response.data), correct call args (route, path with interpolated params, methodId)
- For array returns, mock with an array and verify result is an array
- Include `@ts-expect-error` type test confirming RegionalRoute (e.g., `'americas'`) cannot be passed to these PlatformRoute-only methods
  </action>
  <verify>
    <automated>cd /Users/wardbox/git/whisper && pnpm vitest run src/lol/champion-mastery-v4.test.ts src/lol/champion-v3.test.ts src/lol/summoner-v4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - champion-mastery-v4.ts contains `export const championMasteryV4 =` with 4 methods
    - champion-v3.ts contains `export const championV3 =` with 1 method
    - summoner-v4.ts contains `export const summonerV4 =` with exactly 2 methods (getByPuuid, getByAccountId)
    - summoner-v4.ts does NOT contain getBySummonerName or getBySummonerId or getByMe
    - All 3 modules import PlatformRoute (not RegionalRoute)
    - All 3 modules have `as const` on exported object
    - All 3 modules have `@example` TSDoc blocks on every method
    - All 3 test files contain `@ts-expect-error` for regional route rejection
    - All 3 test suites pass
  </acceptance_criteria>
  <done>3 endpoint modules with 7 total methods, all using PlatformRoute, all tests green, no removed endpoints present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lol-status-v4 and spectator-v5 modules with tests</name>
  <files>
    packages/whisper/src/lol/lol-status-v4.ts
    packages/whisper/src/lol/lol-status-v4.test.ts
    packages/whisper/src/lol/spectator-v5.ts
    packages/whisper/src/lol/spectator-v5.test.ts
  </files>
  <read_first>
    packages/whisper/src/core/client.ts
    packages/whisper/src/types/generated/lol.ts
    packages/whisper/src/types/overrides/lol-spectator.ts
    packages/whisper/src/types/platform.ts
    scripts/generate-schema/registry.ts
  </read_first>
  <action>
Create 2 more Platform-routed endpoint modules.

**lol-status-v4.ts** -- `export const lolStatusV4`:
1. `getStatus(client, route)` -> `Promise<LolPlatformData>` -- path: `/lol/status/v4/platform-data`, methodId: `lol-status-v4.getStatus`

Import `LolPlatformData` from `'../types/generated/lol.js'`.

**spectator-v5.ts** -- `export const spectatorV5`:
1. `getCurrentGame(client, route, puuid)` -> `Promise<LolCurrentGameInfo>` -- path: `/lol/spectator/v5/active-games/by-summoner/${puuid}`, methodId: `spectator-v5.getCurrentGame`
2. `getFeaturedGames(client, route)` -> `Promise<FeaturedGames>` -- path: `/lol/spectator/v5/featured-games`, methodId: `spectator-v5.getFeaturedGames`

Import `LolCurrentGameInfo` from `'../types/generated/lol.js'` and `FeaturedGames` from `'../types/overrides/lol-spectator.js'`.

IMPORTANT: This is spectator-v5 only. Do NOT reference spectator-v4 anywhere (it is deprecated/replaced).

Same pattern: TSDoc on everything, PlatformRoute, `as const`, `.js` import extensions.

**Tests:** Same mockClient pattern. Verify path, methodId, return value for each method. Include `@ts-expect-error` for RegionalRoute.
  </action>
  <verify>
    <automated>cd /Users/wardbox/git/whisper && pnpm vitest run src/lol/lol-status-v4.test.ts src/lol/spectator-v5.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lol-status-v4.ts contains `export const lolStatusV4 =` with 1 method
    - spectator-v5.ts contains `export const spectatorV5 =` with 2 methods
    - spectator-v5.ts imports FeaturedGames from `'../types/overrides/lol-spectator.js'`
    - spectator-v5.ts does NOT contain any reference to "spectator-v4"
    - Both modules import PlatformRoute
    - Both modules have `as const` and TSDoc with `@example`
    - Both test files contain `@ts-expect-error` for regional route
    - Both test suites pass
  </acceptance_criteria>
  <done>2 endpoint modules with 3 total methods, all tests green, spectator uses v5 only.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/lol/champion-mastery-v4.test.ts src/lol/champion-v3.test.ts src/lol/summoner-v4.test.ts src/lol/lol-status-v4.test.ts src/lol/spectator-v5.test.ts` -- all pass
- `npx tsc --noEmit` passes
- No references to removed endpoints in any file
</verification>

<success_criteria>
- 5 endpoint namespace objects exported, 10 total methods
- All use PlatformRoute enforcement
- All methods have TSDoc with examples
- All tests pass including type tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-lol-and-shared-endpoints/04-02-SUMMARY.md`
</output>
