// Auto-generated by generate-schema. Do not edit.


/** Ability effects for a player's agent abilities in a round. */
export interface Ability {
  /** Effect description for the first ability (C key). */
  ability1Effects: string;
  /** Effect description for the second ability (Q key). */
  ability2Effects: string;
  /** Effect description for the grenade ability (E key). */
  grenadeEffects: string;
  /** Effect description for the ultimate ability (X key). */
  ultimateEffects: string;
}

/** Number of times each ability was cast by a player. */
export interface AbilityCasts {
  /** Number of first ability (C key) casts. */
  ability1Casts: number;
  /** Number of second ability (Q key) casts. */
  ability2Casts: number;
  /** Number of grenade ability (E key) casts. */
  grenadeCasts: number;
  /** Number of ultimate ability (X key) casts. */
  ultimateCasts: number;
}

/** A competitive act (season) in Valorant. */
export interface Act {
  /** Unique identifier for the act. */
  id: string;
  /** Whether this act is currently active. */
  isActive: boolean;
  /** Localized display names keyed by locale code. */
  localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string };
  /** Internal name of the act. */
  name: string;
}

/** A coach in a Valorant match (Premier/tournament matches). */
export interface Coach {
  /** Encrypted PUUID of the coach. */
  puuid: string;
  /** Team identifier the coach belongs to. */
  teamId: string;
}

/** All game content available in Valorant for a given locale. */
export interface Content {
  /** Available competitive acts (seasons). */
  acts: ({ id: string; isActive: boolean; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available agent characters. */
  characters: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available charm levels. */
  charmLevels: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available charms. */
  charms: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available weapon skin chromas. */
  chromas: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available equipment items (armor, etc.). */
  equips: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available game modes. */
  gameModes: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available maps. */
  maps: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available player card cosmetics. */
  playerCards: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available player title cosmetics. */
  playerTitles: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available weapon skin upgrade levels. */
  skinLevels: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available weapon skins. */
  skins: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available spray upgrade levels. */
  sprayLevels: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Available spray cosmetics. */
  sprays: ({ assetName: string; assetPath: string; id: string; localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string }; name: string })[];
  /** Current content version string. */
  version: string;
}

/** A content item with asset information and localized names. */
export interface ContentItem {
  /** Internal asset name. */
  assetName: string;
  /** Path to the asset resource. */
  assetPath: string;
  /** Unique identifier for the content item. */
  id: string;
  /** Localized display names keyed by locale code. */
  localizedNames: { "ar-AE": string; "de-DE": string; "en-GB": string; "en-US": string; "es-ES": string; "es-MX": string; "fr-FR": string; "id-ID": string; "it-IT": string; "ja-JP": string; "ko-KR": string; "pl-PL": string; "pt-BR": string; "ru-RU": string; "th-TH": string; "tr-TR": string; "vi-VN": string; "zh-CN": string; "zh-TW": string };
  /** Display name of the content item. */
  name: string;
}

/** Damage dealt to a specific player in a round. */
export interface Damage {
  /** Number of body shots landed. */
  bodyshots: number;
  /** Total damage dealt. */
  damage: number;
  /** Number of headshots landed. */
  headshots: number;
  /** Number of leg shots landed. */
  legshots: number;
  /** Encrypted PUUID of the player who received the damage. */
  receiver: string;
}

/** A player's economy state for a round. */
export interface Economy {
  /** Armor identifier equipped this round. */
  armor: string;
  /** Total loadout value in credits. */
  loadoutValue: number;
  /** Credits remaining after purchases. */
  remaining: number;
  /** Credits spent this round. */
  spent: number;
  /** Weapon identifier equipped this round. */
  weapon: string;
}

/** Information about the finishing blow that killed a player. */
export interface FinishingDamage {
  /** Identifier of the item that dealt the finishing damage. */
  damageItem: string;
  /** Type of damage that killed the player (e.g., "Weapon", "Bomb", "Ability"). */
  damageType: string;
  /** Whether the kill used the weapon's secondary fire mode. */
  isSecondaryFireMode: boolean;
}

/** A kill event in a Valorant match round. */
export interface Kill {
  /** Encrypted PUUIDs of players who assisted in the kill. */
  assistants: string[];
  /** Details about the finishing blow. */
  finishingDamage: { damageItem: string; damageType: string; isSecondaryFireMode: boolean };
  /** Encrypted PUUID of the player who made the kill. */
  killer: string;
  /** Positions and view angles of all players at the time of the kill. */
  playerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[];
  /** Milliseconds since the game started when the kill occurred. */
  timeSinceGameStartMillis: number;
  /** Milliseconds since the round started when the kill occurred. */
  timeSinceRoundStartMillis: number;
  /** Encrypted PUUID of the player who was killed. */
  victim: string;
  /** Map coordinates where the victim died. */
  victimLocation: { x: number; y: number };
}

/** Ranked leaderboard data for a specific competitive act. */
export interface Leaderboard {
  /** Unique identifier of the competitive act. */
  actId: string;
  /** Leaderboard player entries sorted by rank. */
  players: ({ competitiveTier: number; gameName: string; leaderboardRank: number; numberOfWins: number; puuid: string; rankedRating: number; tagLine: string })[];
  /** Search query used to filter the leaderboard (empty string if no filter). */
  query: string;
  /** Shard identifier this leaderboard was retrieved from. */
  shard: string;
  /** Tier detail information for ranks on this leaderboard. */
  tierDetails: ({  })[];
  /** Total number of ranked players on the leaderboard. */
  totalPlayers: number;
}

/** Localized display names keyed by locale code. */
export interface LocalizedNames {
  'ar-AE': string;
  'de-DE': string;
  'en-GB': string;
  'en-US': string;
  'es-ES': string;
  'es-MX': string;
  'fr-FR': string;
  'id-ID': string;
  'it-IT': string;
  'ja-JP': string;
  'ko-KR': string;
  'pl-PL': string;
  'pt-BR': string;
  'ru-RU': string;
  'th-TH': string;
  'tr-TR': string;
  'vi-VN': string;
  'zh-CN': string;
  'zh-TW': string;
}

/** A 2D map coordinate. */
export interface Location {
  /** Horizontal position on the map. */
  x: number;
  /** Vertical position on the map. */
  y: number;
}

/** Metadata about a Valorant match. */
export interface MatchInfo {
  /** Name of the custom game (empty string for non-custom games). */
  customGameName: string;
  /** Duration of the match in milliseconds. */
  gameLengthMillis: number;
  /** Game mode played in this match. */
  gameMode: 'CLASSIC' | 'ODIN' | 'ARAM' | 'TUTORIAL' | 'URF' | 'DOOMBOTSTEEMO' | 'ONEFORALL' | 'ASCENSION' | 'FIRSTBLOOD' | 'KINGPORO' | 'SIEGE' | 'ASSASSINATE' | 'ARSR' | 'DARKSTAR' | 'STARGUARDIAN' | 'PROJECT' | 'GAMEMODEX' | 'NEXUSBLITZ' | 'ULTBOOK' | 'CHERRY' | 'STRAWBERRY';
  /** Epoch timestamp in milliseconds when the match started. */
  gameStartMillis: number;
  /** Game client version used for the match. */
  gameVersion: string;
  /** Whether the match completed normally. */
  isCompleted: boolean;
  /** Whether this was a ranked match. */
  isRanked: boolean;
  /** Identifier of the map played. */
  mapId: '11' | '12' | '21' | '22' | '30';
  /** Unique match identifier. */
  matchId: string;
  /** Premier match metadata (empty for non-Premier matches). */
  premierMatchInfo: Record<string, unknown>[];
  /** Internal provisioning flow identifier. */
  provisioningFlowId: string;
  /** Queue identifier for the match type. */
  queueId: string;
  /** Region where the match was played. */
  region: string;
  /** Season identifier when the match was played. */
  seasonId: string;
}

/** A player's match history with match references. */
export interface Matchlist {
  /** List of match history entries. */
  history: ({ gameStartTimeMillis: number; matchId: string; queueId: string })[];
  /** Encrypted PUUID of the player. */
  puuid: string;
}

/** A single entry in a player's match history. */
export interface MatchlistEntry {
  /** Epoch timestamp in milliseconds when the match started. */
  gameStartTimeMillis: number;
  /** Unique match identifier. */
  matchId: string;
  /** Queue identifier for the match type. */
  queueId: string;
}

/** A player in a Valorant match. */
export interface Player {
  /** Player's account level. */
  accountLevel: number;
  /** Identifier of the agent character played. */
  characterId: string;
  /** Competitive tier at time of the match. */
  competitiveTier: number;
  /** Player's Riot game name. */
  gameName: string;
  /** Whether this player was observing the match. */
  isObserver: boolean;
  /** Identifier of the party the player queued with. */
  partyId: string;
  /** Identifier of the player card cosmetic equipped. */
  playerCard: string;
  /** Identifier of the player title cosmetic equipped. */
  playerTitle: string;
  /** Encrypted PUUID of the player. */
  puuid: string;
  /** Aggregate performance statistics for the match. */
  stats: { abilityCasts: { ability1Casts: number; ability2Casts: number; grenadeCasts: number; ultimateCasts: number }; assists: number; deaths: number; kills: number; playtimeMillis: number; roundsPlayed: number; score: number };
  /** Player's Riot tag line. */
  tagLine: string;
  /** Team identifier the player belongs to. */
  teamId: string;
}

/** Player position and view angle at a moment in time. */
export interface PlayerLocations {
  /** Map coordinates of the player. */
  location: { x: number; y: number };
  /** Encrypted PUUID of the player. */
  puuid: string;
  /** Direction the player was looking in radians. */
  viewRadians: number;
}

/** A player's detailed statistics for a single round. */
export interface PlayerRoundStats {
  /** Ability effects triggered during the round. */
  ability: { ability1Effects: string; ability2Effects: string; grenadeEffects: string; ultimateEffects: string };
  /** Damage dealt to other players during the round. */
  damage: ({ bodyshots: number; damage: number; headshots: number; legshots: number; receiver: string })[];
  /** Economy state for the round (credits, loadout). */
  economy: { armor: string; loadoutValue: number; remaining: number; spent: number; weapon: string };
  /** Kills made during the round. */
  kills: ({ assistants: string[]; finishingDamage: { damageItem: string; damageType: string; isSecondaryFireMode: boolean }; killer: string; playerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[]; timeSinceGameStartMillis: number; timeSinceRoundStartMillis: number; victim: string; victimLocation: { x: number; y: number } })[];
  /** Encrypted PUUID of the player. */
  puuid: string;
  /** Score earned during the round. */
  score: number;
}

/** Aggregate performance statistics for a player in a match. */
export interface PlayerStats {
  /** Total ability casts across all rounds. */
  abilityCasts: { ability1Casts: number; ability2Casts: number; grenadeCasts: number; ultimateCasts: number };
  /** Total assists across all rounds. */
  assists: number;
  /** Total deaths across all rounds. */
  deaths: number;
  /** Total kills across all rounds. */
  kills: number;
  /** Total playtime in milliseconds. */
  playtimeMillis: number;
  /** Number of rounds played. */
  roundsPlayed: number;
  /** Total score across all rounds. */
  score: number;
}

/** Recently completed match IDs for a queue. */
export interface RecentMatches {
  /** Server time when this data was retrieved (epoch milliseconds). */
  currentTime: number;
  /** List of recently completed match IDs. */
  matchIds: string[];
}

/** Detailed result of a single round in a match. */
export interface RoundResult {
  /** Encrypted PUUID of the player who defused the bomb (empty if not defused). */
  bombDefuser: string;
  /** Encrypted PUUID of the player who planted the bomb (empty if not planted). */
  bombPlanter: string;
  /** Map coordinates where the bomb was defused. */
  defuseLocation: { x: number; y: number };
  /** Player positions when the bomb was defused. */
  defusePlayerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[];
  /** Milliseconds into the round when the bomb was defused. */
  defuseRoundTime: number;
  /** Map coordinates where the bomb was planted. */
  plantLocation: { x: number; y: number };
  /** Player positions when the bomb was planted. */
  plantPlayerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[];
  /** Milliseconds into the round when the bomb was planted. */
  plantRoundTime: number;
  /** Bomb site where the spike was planted (e.g., "A", "B", "C"). */
  plantSite: string;
  /** Per-player detailed statistics for this round. */
  playerStats: ({ ability: { ability1Effects: string; ability2Effects: string; grenadeEffects: string; ultimateEffects: string }; damage: ({ bodyshots: number; damage: number; headshots: number; legshots: number; receiver: string })[]; economy: { armor: string; loadoutValue: number; remaining: number; spent: number; weapon: string }; kills: ({ assistants: string[]; finishingDamage: { damageItem: string; damageType: string; isSecondaryFireMode: boolean }; killer: string; playerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[]; timeSinceGameStartMillis: number; timeSinceRoundStartMillis: number; victim: string; victimLocation: { x: number; y: number } })[]; puuid: string; score: number })[];
  /** Visual ceremony played at end of round (e.g., "CeremonyDefault"). */
  roundCeremony: string;
  /** Zero-based round number. */
  roundNum: number;
  /** Human-readable round result description. */
  roundResult: string;
  /** Machine-readable round result code. */
  roundResultCode: string;
  /** Team identifier of the winning team. */
  winningTeam: string;
  /** Role of the winning team in this round (e.g., "attack", "defense"). */
  winningTeamRole: string;
}

/** A platform status entry (incident or maintenance). */
export interface Status {
  /** Timestamp when this status entry will be archived. */
  archive_at: string;
  /** Timestamp when this status entry was created. */
  created_at: string;
  /** Unique numeric identifier. */
  id: number;
  /** Severity level of the incident (e.g., "info", "warning", "critical"). */
  incident_severity: string;
  /** Current maintenance status (e.g., "scheduled", "in_progress", "complete"). */
  maintenance_status: string;
  /** Affected platform identifiers. */
  platforms: string[];
  /** Localized title strings for this status entry. */
  titles: ({ content: string; locale: string })[];
  /** Timestamp when this status entry was last updated. */
  updated_at: string;
  /** Chronological updates to this status entry. */
  updates: ({ author: string; created_at: string; id: number; publish: boolean; publish_locations: string[]; translations: ({ content: string; locale: string })[]; updated_at: string })[];
}

/** A team in a Valorant match. */
export interface Team {
  /** Points scored by the team. */
  numPoints: number;
  /** Total rounds played by the team. */
  roundsPlayed: number;
  /** Number of rounds won by the team. */
  roundsWon: number;
  /** Team identifier (e.g., "Blue", "Red"). */
  teamId: string;
  /** Whether this team won the match. */
  won: boolean;
}

/** Tier detail information for ranked leaderboards. */
export interface Tier {
}

/** A single update to a platform status entry. */
export interface Update {
  /** Author of the update. */
  author: string;
  /** Timestamp when the update was created. */
  created_at: string;
  /** Unique numeric identifier for the update. */
  id: number;
  /** Whether the update should be publicly visible. */
  publish: boolean;
  /** Where the update should be published. */
  publish_locations: string[];
  /** Localized translations of the update content. */
  translations: ({ content: string; locale: string })[];
  /** Timestamp when the update was last modified. */
  updated_at: string;
}

/** Complete data for a Valorant match including players, rounds, and teams. */
export interface ValMatch {
  /** Coaches participating in the match (Premier/tournament only). */
  coaches: ({ puuid: string; teamId: string })[];
  /** Match metadata including mode, map, timing, and identifiers. */
  matchInfo: { customGameName: string; gameLengthMillis: number; gameMode: 'CLASSIC' | 'ODIN' | 'ARAM' | 'TUTORIAL' | 'URF' | 'DOOMBOTSTEEMO' | 'ONEFORALL' | 'ASCENSION' | 'FIRSTBLOOD' | 'KINGPORO' | 'SIEGE' | 'ASSASSINATE' | 'ARSR' | 'DARKSTAR' | 'STARGUARDIAN' | 'PROJECT' | 'GAMEMODEX' | 'NEXUSBLITZ' | 'ULTBOOK' | 'CHERRY' | 'STRAWBERRY'; gameStartMillis: number; gameVersion: string; isCompleted: boolean; isRanked: boolean; mapId: '11' | '12' | '21' | '22' | '30'; matchId: string; premierMatchInfo: Record<string, unknown>[]; provisioningFlowId: string; queueId: string; region: string; seasonId: string };
  /** All players in the match with their stats and agent selections. */
  players: ({ accountLevel: number; characterId: string; competitiveTier: number; gameName: string; isObserver: boolean; partyId: string; playerCard: string; playerTitle: string; puuid: string; stats: { abilityCasts: { ability1Casts: number; ability2Casts: number; grenadeCasts: number; ultimateCasts: number }; assists: number; deaths: number; kills: number; playtimeMillis: number; roundsPlayed: number; score: number }; tagLine: string; teamId: string })[];
  /** Detailed results for each round of the match. */
  roundResults: ({ bombDefuser: string; bombPlanter: string; defuseLocation: { x: number; y: number }; defusePlayerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[]; defuseRoundTime: number; plantLocation: { x: number; y: number }; plantPlayerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[]; plantRoundTime: number; plantSite: string; playerStats: ({ ability: { ability1Effects: string; ability2Effects: string; grenadeEffects: string; ultimateEffects: string }; damage: ({ bodyshots: number; damage: number; headshots: number; legshots: number; receiver: string })[]; economy: { armor: string; loadoutValue: number; remaining: number; spent: number; weapon: string }; kills: ({ assistants: string[]; finishingDamage: { damageItem: string; damageType: string; isSecondaryFireMode: boolean }; killer: string; playerLocations: ({ location: { x: number; y: number }; puuid: string; viewRadians: number })[]; timeSinceGameStartMillis: number; timeSinceRoundStartMillis: number; victim: string; victimLocation: { x: number; y: number } })[]; puuid: string; score: number })[]; roundCeremony: string; roundNum: number; roundResult: string; roundResultCode: string; winningTeam: string; winningTeamRole: string })[];
  /** Teams in the match with their scores and win status. */
  teams: ({ numPoints: number; roundsPlayed: number; roundsWon: number; teamId: string; won: boolean })[];
}

/** Platform status data for a Valorant shard. */
export interface ValPlatformData {
  /** Shard identifier (e.g., "NA", "EU"). */
  id: string;
  /** Active incidents affecting the platform. */
  incidents: ({ archive_at: string; created_at: string; id: number; incident_severity: string; maintenance_status: string; platforms: string[]; titles: ({ content: string; locale: string })[]; updated_at: string; updates: ({ author: string; created_at: string; id: number; publish: boolean; publish_locations: string[]; translations: ({ content: string; locale: string })[]; updated_at: string })[] })[];
  /** Supported locale codes. */
  locales: string[];
  /** Scheduled or active maintenance windows. */
  maintenances: ({ archive_at: string; created_at: string; id: number; incident_severity: string; maintenance_status: string; platforms: string[]; titles: ({ content: string; locale: string })[]; updated_at: string; updates: ({ author: string; created_at: string; id: number; publish: boolean; publish_locations: string[]; translations: ({ content: string; locale: string })[]; updated_at: string })[] })[];
  /** Display name of the platform. */
  name: string;
}
